{
  "name": "focus-push-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend for Focus's iOS Web Push reminders",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute focus-push-db --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "web-push-browser": "^1.4.2"
  }
}
